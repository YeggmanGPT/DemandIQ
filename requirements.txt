# --- Data layer ---
duckdb>=0.10.0
pandas>=2.0.0
numpy>=1.24.0

# --- Forecasting (Day 2) ---
prophet>=1.1.5
xgboost>=2.0.0
scikit-learn>=1.3.0

# --- Agent layer (Day 2) ---
langchain>=0.2.0
langchain-groq>=0.1.0
langgraph>=0.2.0
groq>=0.9.0
pydantic>=2.0.0

# --- Automation (Day 3) ---
# NOTE: no APScheduler here on purpose -- see BUILD_LOG.md Module 5.
# An in-process scheduler dies whenever the host app sleeps/restarts, so
# scheduled anomaly checks run as a GitHub Actions cron job (see
# .github/workflows/anomaly-check.yml) calling scheduler/monitor.py instead.
requests>=2.31.0

# --- Dashboard (Day 3) ---
streamlit>=1.32.0
plotly>=5.18.0

# --- Config ---
python-dotenv>=1.0.0

# NOTE on prophet: it depends on cmdstanpy/pystan and is the one package
# in this list that regularly fails a plain `pip install`.
#   - Linux/Mac: if it fails, try `pip install pystan==2.19.1.1` first,
#     then re-run this file.
#   - Windows: `conda install -c conda-forge prophet` is far less painful
#     than pip if you have Anaconda/Miniconda available.
# Budget time for this on Day 1 — don't let it eat into Day 2.
